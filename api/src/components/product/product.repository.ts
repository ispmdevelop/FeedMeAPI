import { GenericRepositoryHelper } from '../../helpers/genericRepository';
import { ProductEntity } from '../../persistance/entity/Product';

export class ProductRepository extends GenericRepositoryHelper<ProductEntity> {}
